<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New fixed expense</h1>

  <%= turbo_frame_tag :fixed_expenses do %>
    <%= render "form", fixed_expense: @fixed_expense %>
  <% end %>

  <%= link_to 'Back to fixed expenses', fixed_expenses_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
